import normalize from "./normalize";

const fontSize = {
  xxsmall: normalize(10),
    xsmall: normalize(12),
    small:normalize(14),
    medium: normalize(16),
    large: normalize(20),
    xlarge:normalize(26),
    xxlarge:normalize(32),
  };
  
  export default fontSize;
  